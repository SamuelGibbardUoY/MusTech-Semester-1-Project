File sends a raw byte of data via a USB cable from the micro controller to the connected PC every second
Can only send up to 1 byte per message
Can use the view Hexi function in coolterm to verify it working
